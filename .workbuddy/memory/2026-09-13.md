# 2026-09-13

## 任务：中国一级气象地理区划交互地图（单文件 HTML）

**交付物**：`D:\my-project\地理\中国气象地理区划交互地图.html`（868 KB，完全自包含，无任何外部资源）

### 关键决策
- **不走腾讯地图代理模式，改用「本地 GeoJSON + 自绘 SVG」**。原因：需求是自绘专题面 + 单文件离线交付，瓦片底图无用且引入 `_TMapSecurityConfig` 代理依赖。此方案不触碰合规红线（红线针对境外瓦片服务），但已在页面显式标注数据来源与「示意图、非权威地图」声明。
- 投影：墨卡托。主图包围盒 lng[73,135.5] lat[17.6,53.8]，viewBox 1000×739；南海诸岛另设附图框（避免 3°N 岛礁把主图包围盒拉长）。

### 权威依据（已核实原文）
- 《中国气象地理区划手册》**11 个一级气象地理区划**：西北 / 华北 / 内蒙古 / 东北 / 黄淮 / 江淮 / 江南 / 江汉 / 华南 / 西南 / 西藏。来源：中国天气网、湖北省气象局科普、金山网报道（均引手册原文）。
- 中国气象局 2021《气象地理区划规范》：另设 10 个「一级地区区划」（华北/东北/华东/华中/华南/西南/西北/内蒙古/西藏/新疆）+ 11 个「特定区域区划」（黄淮、江淮、江南、江汉在内）+ 流域区划。山东属**华东**（易误判为华北）。
- 齐鲁晚报引手册：**山东一省分属华北南部和黄淮北部** —— 用于确定山东的区划拆分。

### 数据
- 行政区划：DataV.GeoAtlas（高德，GCJ-02）。34 省级 + 475 个地级/区级单元。
- 河流：Natural Earth 河道中心线（WGS84）；淮河下游、海河干流、长江河口段、嘉陵江、岷江、乌江、钱塘江、瓯江、南渡江为按已知走向补的示意线。
- 自建内容层：100 条山脉注记、17 个湖泊、11 区划 → 地级市归属映射、34 省要素清单。

### 区划归属的近似处理（已在页面披露）
一级区划原生界线是黄河/淮河/长江/南岭等自然线，本图按**地级市整体聚合**绘制。
6 个跨区划省按主体归并：江苏、安徽、山东、河南、湖北、福建。例：江苏淮安/盐城→江淮，南京→江南；河南南阳→江汉、信阳→江淮。

### 产出管线（可复现）
`build/` 下：`fetch_national.py` `fetch_cities.py` `fetch_rivers.py` `geo.py` `content.py` `assemble.py` `template.html` + 3 个 Playwright 测试脚本。
原始数据在 `data/`（约 13 MB，含 ne_rivers.geojson 7.3 MB）。

### 环境要点（补充到跨项目记忆）
- DataV GeoAtlas 会抛 `SSL: UNEXPECTED_EOF_WHILE_READING`，**必须重试退避**；`curl -o` 在本机写盘失败，用 Python urllib。
- **验证单文件 HTML 用全局 `@playwright/test` + 系统 Chrome**：
  `NODE_PATH="$APPDATA/npm/node_modules" <managed node> script.js`，`chromium.launch({channel:'chrome',headless:true})`。无需下载 Chromium。
- 页面无 `pageerror` / `console.error` 才可交付 —— `MNaN` 类路径错误只在这里暴露。

### 已沉淀技能
`~/.workbuddy/skills/china-offline-interactive-map/SKILL.md`（含 9 个必踩的坑与验证规则）

### 最终功能集（经 Playwright 逐项验证，控制台零错误）
- 省/地两级界线；「显示全国市界」开关一次性生成 685 个子路径（单 path，d 长 448 KB，构建约 30 ms，开/关仅切 display）
- 点击省份 → 面板列出所属一级区划（含「部分/全境」标记与纳入的地级市清单）+ 山脉 + 河流湖泊 + 主要城市/下辖单元，全部可勾选
- 勾选区划 → 粗线整体框出全国范围（描边+同色填充的合框技巧）；勾选要素 → 河流线/山脉三角/湖泊圆点/城市面高亮
- 滚轮缩放（viewBox 变换）、拖拽平移、双击复位；省名可与区划名自动避让
- 南海诸岛附图（九段线 + 6 个岛群符号），主图按「外环最大纬度 < 17.8° 剔除」排除岛礁

### 排查记录
- 截图目视误判「市界未渲染」——实为截图缩放后 1.5 px 浅色虚线不可辨。**结论：低对比度细线必须在高倍放大 + deviceScaleFactor:2 下截图复核**，或用 getTotalLength()/子路径数做程序化确认。

### 迭代 2（用户反馈）：山脉改为「区域」显示
- 用户指出三角形符号「连走向都看不出来」。改为**按山系真实走向绘制的示意范围多边形**（`build/mtn_shapes.py`，100 条，全部手工按 NE-SW / NW-SE / E-W / N-S 走向给定顶点）。
- MT 注册表由 `[名称, 经度, 纬度]` 扩展为 `[名称, 经度, 纬度, 扁平整数多边形]`；content.py 增加齐备性断言（缺失/多余/顶点<3 均直接报错）。
- 前端 `renderFeats` 中 `m:` 分支由三角符号改为 `<path class="mka">`（fill #c0603a @ .34，stroke #a04a20）；标签仍取原「轴向中心点」+8px。
- 嵌套山体（如太白山 ⊂ 秦岭、华山 ⊂ 秦岭）会叠加变深，属预期效果。
- 文件 868 KB → 878 KB。已在页脚改述为「山系不存在法定界线，按真实走向绘制示意范围；小面积名山适度放大以保证可读性」。


